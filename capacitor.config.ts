import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'crypto-wallet-fe',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
